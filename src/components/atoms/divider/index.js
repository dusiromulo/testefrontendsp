import React from 'react';
import { View } from 'react-native';

const Divider = () => {
    return (
        <View style={{
            width: '100%',
            height: 1,
            backgroundColor: '#CCCCCC'
        }}/>
    );
}

export default Divider;